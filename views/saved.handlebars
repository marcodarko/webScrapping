<div class="jumbotron text-center" id="hero"> 
        <h1>My Articles</h1>
        <p>The New York Times</p>
</div>
<div class="container" id="savedContainer">
  <!-- <table class="table">
     {{#if articles}}
      <tbody>
        {{#each articles}}
         
          <tr>
            <td><a href="{{this.link}}" target="_blank">{{this.headline}}</a></td>
            <td>{{this.summary}}</td>
            {{#if this.comments}}
            <td>{{this.comments}}</td>
            {{else}}
            <td> No Comments yet</td>
            {{/if}}
            <td>
            <form action="/comment/{{this._id}}?_method=PUT" method="POST">
            <button type="submit" class="btn btn-default navbar-btn">ADD COMMENT</button>
            </td>
          </tr>
        {{/each}}

      </tbody>
      {{else}}
      <td class="text-center"> Looks like you haven't saved any articles</td>
      {{/if}}
    

    </table> -->

	{{#if articles}}
	{{#each articles}}
		<div class="jumbotron savedArticle">
			<span class="date">{{this.date}}</span>
			<h3><a href="{{this.link}}" target="_blank">{{this.headline}}</a></h3>
			<p>{{this.summary}}</p>
			<hr>
			
				<h3>Comments</h3>
				{{#each this.comments}}
					<p><span class="glyphicon glyphicon-comment" aria-hidden="true"></span> {{this.comment}}</p>
				{{/each}}
			<hr>
			<form action="/comment/{{this._id}}?_method=PUT" method="POST">
				<textarea style="width: 100%" placeholder="Comment on this" name="comment" class="textArea">
					
				</textarea>
				<br>
				<br>
				<button class="btn btn-default" type="submit"><span class="glyphicon glyphicon-ok" aria-hidden="true"></span> SAVE COMMENT</button>
			</form>
		
			 <form action="/remove/{{this._id}}?_method=PUT" method="POST">
            <button type="submit" class="btn btn-default navbar-btn"><span class="glyphicon glyphicon-minus" aria-hidden="true"></span> REMOVE ARTICLE</button>
            </form>
			
		</div>
	
	{{/each}}
	{{else}}
	<div class="jumbotron text-center" id="noSaved"> 
        <h3>No Saved Articles</h3>
	</div>
	{{/if}}

  </div>
